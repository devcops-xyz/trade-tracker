# متتبع التجارة - Trade Tracker Web App

تطبيق ويب بسيط لتتبع الصادرات والواردات وحساب الربح اليومي والأسبوعي والشهري.

## المميزات ✨

- ✅ واجهة مستخدم بالعربية مع دعم RTL كامل
- ✅ إضافة معاملات الصادرات والواردات
- ✅ حساب الربح تلقائياً (يومي، أسبوعي، شهري)
- ✅ حفظ البيانات محلياً في المتصفح (localStorage)
- ✅ تصميم متجاوب يعمل على جميع الأجهزة
- ✅ لا يحتاج خادم أو قاعدة بيانات
- ✅ يعمل بدون اتصال بالإنترنت بعد التحميل الأول

## التشغيل المحلي 🚀

ببساطة افتح ملف `index.html` في المتصفح!

## النشر على الإنترنت مجاناً 🌐

### 1. Netlify (الأسهل - موصى به)

```bash
# طريقة السحب والإفلات
1. اذهب إلى https://app.netlify.com/drop
2. اسحب مجلد TradeTrackerWeb بالكامل
3. ستحصل على رابط مباشر فوراً!
```

أو باستخدام سطر الأوامر:
```bash
npm install -g netlify-cli
cd TradeTrackerWeb
netlify deploy --prod
```

### 2. Vercel

```bash
npm install -g vercel
cd TradeTrackerWeb
vercel --prod
```

### 3. GitHub Pages

```bash
# قم بإنشاء repository جديد على GitHub
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin YOUR_REPO_URL
git push -u origin main

# ثم فعّل GitHub Pages من إعدادات الـ repository
```

### 4. Surge

```bash
npm install -g surge
cd TradeTrackerWeb
surge
```

## البنية 📁

```
TradeTrackerWeb/
├── index.html      # الصفحة الرئيسية
├── style.css       # التنسيقات والألوان
├── app.js          # المنطق والحسابات
└── README.md       # التوثيق
```

## كيفية الاستخدام 📖

1. **إضافة معاملة:**
   - اختر نوع المعاملة (صادرات أو واردات)
   - أدخل المبلغ
   - أدخل الوصف
   - اضغط "إضافة معاملة"

2. **عرض الأرباح:**
   - يتم حساب الربح تلقائياً
   - الأخضر = ربح
   - الأحمر = خسارة

3. **حذف المعاملات:**
   - اضغط على زر الحذف لكل معاملة
   - أو "حذف الكل" لمسح كل المعاملات

## التقنيات المستخدمة 🛠

- HTML5
- CSS3 (Flexbox, Grid, Gradients)
- Vanilla JavaScript (ES6+)
- LocalStorage API

## الترخيص 📄

مفتوح المصدر - استخدم بحرية!
