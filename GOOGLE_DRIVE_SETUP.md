# إعداد النسخ الاحتياطي على Google Drive

## الخطوات المطلوبة

### 1. إنشاء مشروع Google Cloud

1. اذهب إلى: https://console.cloud.google.com/
2. أنشئ مشروع جديد أو اختر مشروع موجود
3. سجل اسم المشروع

### 2. تفعيل Google Drive API

1. اذهب إلى "APIs & Services" > "Library"
2. ابحث عن "Google Drive API"
3. اضغط على "Enable"

### 3. إنشاء OAuth 2.0 Credentials

1. اذهب إلى "APIs & Services" > "Credentials"
2. اضغط "Create Credentials" > "OAuth client ID"
3. اختر "Web application"
4. أضف Authorized JavaScript origins:
   - `http://localhost:8080` (للتطوير)
   - رابط موقعك الحقيقي (مثال: `https://your-app.netlify.app`)
5. أضف Authorized redirect URIs (نفس الروابط أعلاه)
6. اضغط "Create"
7. **انسخ Client ID** (سيكون بصيغة: `xxxxx.apps.googleusercontent.com`)

### 4. تعديل الملف `google-drive.js`

افتح ملف `google-drive.js` وعدّل السطر:

```javascript
this.CLIENT_ID = 'YOUR_CLIENT_ID_HERE.apps.googleusercontent.com';
```

واستبدل `YOUR_CLIENT_ID_HERE` بالـ Client ID الذي نسخته.

### 5. رفع التطبيق

⚠️ **مهم**: Google Sign-In لا يعمل على `file://` - يجب رفع التطبيق على استضافة حقيقية.

**خيارات الاستضافة المجانية:**

#### Netlify (موصى به)
```bash
cd TradeTrackerWeb
npx netlify-cli deploy --prod
```

#### Vercel
```bash
npx vercel TradeTrackerWeb --prod
```

#### GitHub Pages
```bash
cd TradeTrackerWeb
gh repo create trade-tracker --public --source=. --push
# ثم فعّل Pages من إعدادات المستودع
```

### 6. تحديث OAuth Origins

بعد رفع التطبيق، ارجع إلى Google Cloud Console:

1. اذهب إلى "APIs & Services" > "Credentials"
2. اضغط على OAuth 2.0 Client ID الذي أنشأته
3. أضف رابط التطبيق الجديد إلى:
   - Authorized JavaScript origins
   - Authorized redirect URIs
4. احفظ التغييرات

### 7. اختبار النسخ الاحتياطي

1. افتح التطبيق في المتصفح
2. اضغط "🔐 تسجيل الدخول بجوجل"
3. اختر حساب Google
4. امنح الصلاحيات المطلوبة
5. اضغط "☁️ نسخ احتياطي" لحفظ البيانات
6. اضغط "📥 استعادة" لاستعادة البيانات

## كيف يعمل النظام؟

- **النسخ الاحتياطي**: يحفظ جميع المعاملات في ملف JSON على Google Drive
- **الاستعادة**: يقرأ الملف من Google Drive ويستبدل البيانات المحلية
- **اسم الملف**: `trade-tracker-backup.json`
- **الموقع**: في مجلد "My Drive" الخاص بك

## استكشاف الأخطاء

### خطأ: "يرجى تسجيل الدخول أولاً"
- تأكد من تسجيل الدخول بحساب Google أولاً

### خطأ: "Popup blocked"
- اسمح للنوافذ المنبثقة في متصفحك

### خطأ: "Origin mismatch"
- تأكد من إضافة رابط موقعك إلى Authorized origins في Google Console

### خطأ: "لم يتم العثور على نسخة احتياطية"
- تأكد من عمل نسخة احتياطية أولاً قبل الاستعادة

## الأمان

- يتم تخزين Token في localStorage فقط
- لا يتم إرسال البيانات لأي خادم آخر
- البيانات محفوظة في حساب Google Drive الخاص بك فقط
- يمكنك إلغاء الصلاحيات في أي وقت من: https://myaccount.google.com/permissions

## للتطوير المحلي

إذا أردت الاختبار محلياً:

```bash
cd TradeTrackerWeb
python3 -m http.server 8080
```

ثم افتح: http://localhost:8080

تأكد من إضافة `http://localhost:8080` إلى Authorized origins!
